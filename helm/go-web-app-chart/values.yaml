# Default values for go-web-app-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: docker.io/aakash6012/go-lang-app
  pullPolicy: Always
  tag: "16956043243"

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: go-web-app.local
      paths:
        - path: /
          pathType: Prefix

